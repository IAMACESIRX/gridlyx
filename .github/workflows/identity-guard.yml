name: Gridelyx Identity Guard

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  identity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1
      - name: Validate current-tree identity
        run: python tools/terminology_check.py
