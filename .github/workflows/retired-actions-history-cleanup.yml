name: Gridelyx Actions History Cleanup

on:
  push:
    paths:
      - '.github/workflows/retired-actions-history-cleanup.yml'

permissions:
  actions: write
  contents: read

jobs:
  purge:
    runs-on: ubuntu-latest
    steps:
      - name: Delete workflow runs predating verified identity scrub
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          CURRENT_RUN_ID: ${{ github.run_id }}
          CLEAN_CUTOFF: '2026-08-28T08:45:45Z'
        shell: bash
        run: |
          python - <<'PY'
          import datetime as dt
          import json
          import os
          import urllib.error
          import urllib.request

          token = os.environ['GH_TOKEN']
          repo = os.environ['REPOSITORY']
          current = int(os.environ['CURRENT_RUN_ID'])
          cutoff = dt.datetime.fromisoformat(os.environ['CLEAN_CUTOFF'].replace('Z', '+00:00'))
          headers = {
              'Authorization': f'Bearer {token}',
              'Accept': 'application/vnd.github+json',
              'X-GitHub-Api-Version': '2022-11-28',
              'User-Agent': 'gridelyx-actions-history-cleanup',
          }

          def request(url: str, method: str = 'GET'):
              req = urllib.request.Request(url, headers=headers, method=method)
              with urllib.request.urlopen(req) as response:
                  payload = response.read()
                  return json.loads(payload) if payload else None

          deleted = 0
          page = 1
          while True:
              url = f'https://api.github.com/repos/{repo}/actions/runs?per_page=100&page={page}'
              data = request(url)
              runs = data.get('workflow_runs', []) if data else []
              if not runs:
                  break
              for run in runs:
                  run_id = int(run['id'])
                  if run_id == current:
                      continue
                  created = dt.datetime.fromisoformat(run['created_at'].replace('Z', '+00:00'))
                  if created >= cutoff:
                      continue
                  try:
                      request(
                          f'https://api.github.com/repos/{repo}/actions/runs/{run_id}',
                          method='DELETE',
                      )
                      deleted += 1
                  except urllib.error.HTTPError as exc:
                      if exc.code != 404:
                          raise
              page += 1

          print(f'Deleted {deleted} workflow runs predating {cutoff.isoformat()}.')
          PY
